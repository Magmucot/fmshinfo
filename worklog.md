# Worklog — Проект «СУНЦ Инфо» (агрегатор информации СУНЦ НГУ)

---
Task ID: 1
Agent: main (Z.ai Code)
Task: Комплексный анализ сайтов СУНЦ НГУ и НГУ, поиск источников данных (столовая, дежурства, звонки, ночные вожатые, погода, полезная информация)

Work Log:
- Проведён веб-поиск (10+ запросов) по темам: расписание звонков, меню столовой, дежурства, ночные вожатые, расписание занятий, новости
- Прочитаны и проанализированы страницы: sesc.nsu.ru/sveden/catering (меню PDF), /education/academic-calendar (звонки), /media/news (новости), /faq, table-sesc.nsu.ru (SPA расписания)
- Найдено и протестировано открытое API расписания: https://table-sesc.nsu.ru/api/bell, /api/school-class, /api/schedule/find?group=X (JSON, без авторизации)
- Скачан и распарсен реальный PDF меню menu_30.08.26.pdf (pdftotext работает): структура «Завтрак/Обед/Полдник/Ужин/Второй ужин», КБЖУ, ингредиенты, итоги
- Проверены погодные API: Open-Meteo (координаты 54.842/83.098, лимит на сегодня исчерпан) и wttr.in (работает, JSON)
- УСТАНОВЛЕНО: дежурства и ночные вожатые онлайн НЕ публикуются (стенды/внутренние каналы) → модули с ручным администрированием
- Составлен подробный аналитический документ: /home/z/my-project/docs/sunc-info-analysis.md (спецификация для сайта/ТГ-бота)

Stage Summary:
- Все источники данных зафиксированы: меню (PDF через /sveden/catering), звонки+расписание (table-sesc API), новости (HTML /media/news), погода (Open-Meteo/wttr.in), дежурства и вожатые (ручной ввод, БД)
- Ключевой документ-спецификация готов: docs/sunc-info-analysis.md
- Следующие шаги: Task 2 (FastAPI-эталон, субагент), Task 3+4 (Next.js бэкенд и фронтенд, main), тестирование, cron

---
Task ID: 2
Agent: main (Z.ai Code) + general-purpose (частично)
Task: FastAPI эталонный бэкенд с парсерами и мини-сайтом (fastapi-backend/)

Work Log:
- Субагент создал: parsers/ (menu, schedule, news, weather), models.py, database.py, requirements.txt
- Субагент превысил таймаут → main дописал: main.py (19 роутов, TTL-кэш, CORS, X-Admin-Key), parsers/info_data.py, seed.py, README.md, static/index.html
- Исправлен критический баг: regex ссылок PDF-меню — Битрикс использует одинарные кавычки href='...' → добавлена поддержка обоих типов кавычек
- Сквозные тесты на РЕАЛЬНЫХ данных: меню 30.08.2026 (4 приёма пищи, 25 блюд, 2866 ккал/день), звонки (13), классы (25), расписание 10-1, новости (12 шт. с датами), погода (16°C через wttr.in)
- py_compile OK, импорт FastAPI-приложения OK, seed.py OK (7 дежурств + 12 вожатых)

Stage Summary:
- Эталонная реализация FastAPI ПОЛНОСТЬЮ ГОТОВА и протестирована: /home/z/my-project/fastapi-backend/
- Все парсеры работают с реальными источниками; Swagger: /docs; мини-сайт: static/index.html
- Следующий этап: основное Next.js-приложение (демо в этом окружении)

---
Task ID: 3
Agent: main (Z.ai Code)
Task: Бэкенд Next.js: Prisma-схема, парсеры источников, 11 API-роутов

Work Log:
- Prisma: модели DutyEntry, NightCounselor, Feedback → bun run db:push
- src/lib/server/: cache.ts (TTL-кэш + stale-режим), sources.ts (URL, UA, статич. звонки, справочник), menu.ts (парсер PDF), tableSesc.ts (клиент API расписания), news.ts (парсер новостей), weather.ts (Open-Meteo→wttr.in), seed.ts (демо-дежурства/вожатые на 14 дней)
- API-роуты: /api/menu, /api/bells, /api/classes, /api/schedule, /api/weather, /api/news, /api/duty (GET/POST/DELETE), /api/counselors (GET/POST/DELETE), /api/feedback, /api/info, /api/document
- Парсер меню отлажен на реальных PDF: исправлены 5 багов — (1) одинарные кавычки href в Битриксе, (2) \b не работает с кириллицей в JS regex, (3) «Углеводы-91» классифицировался как noise, (4) normalizeDate обрезала год «2026»→«26», (5) не распознавались секции «Вт.завтрак»/«Вт. ужин» и «Итого за Ккал-4 129» (итог дня)
- Итог парсинга: 30.08 → 2866 ккал ✓, 01.09 → 6 секций 4129 ккал ✓, 08.09 → 4241 ккал ✓ (совпадает с собственными итогами PDF)

Stage Summary:
- Все 11 API-эндпоинтов работают на РЕАЛЬНЫХ данных (проверено curl + браузер)
- Парсер меню — полный конвейер: каталог → PDF (pdftotext) → 6 секций приёмов пищи с КБЖУ и ингредиентами

---
Task ID: 4
Agent: main (Z.ai Code)
Task: Фронтенд: одностраничный портал «СУНЦ Инфо» на /

Work Log:
- Тема: тёплая amber/stone палитра (globals.css, oklch), шрифт Manrope (кириллица), тёмная тема next-themes, кастомные скроллбары, .doc-prose для markdown
- src/components/sunc/: app.tsx (шапка с живыми часами НСК, погодным чипом, 9 вкладок, липкий футер), api.ts (React Query хуки), types.ts, shared.tsx
- Секции: dashboard (погода, «Сейчас в школе» с прогрессом пары, калории, превью меню, дежурства, вожатые, новости), canteen (полное меню с выбором дат из каталога, КБЖУ-бейджи, составы, итоги дня), schedule (звонки по парам + сетка Пн-Сб по 25 классам), duty (7 дней + админ-панель X-Admin-Key), counselors (карточки общежитий + экстренные контакты + админ), weather (текущая + прогноз 3 дня), news, info (контакты/ссылки/форма обратной связи), document (react-markdown + remark-gfm)
- Исправления по итогам QA: remark-gfm 4.x = default export; setState-in-effect заменён на CSS dark:hidden и производное значение; WEEKDAYS[0]=«Воскресенье»; seed вожатых в воскресенья

Stage Summary:
- Верифицировано через agent-browser: все 9 вкладок с реальными данными, форма обратной связи (запись в БД ✓), тёмная тема ✓, мобайл 390px без переполнения ✓, липкий футер ✓, ошибок в консоли нет, lint чистый
- Полный цикл: анализ → документ → FastAPI-эталон → рабочее Next.js-приложение с реальными парсерами

---
Task ID: 5
Agent: main (Z.ai Code)
Task: Финализация: cron-задача, уборка, итоговая верификация

Work Log:
- Создана cron-задача webDevReview (job_id 346534): каждые 15 минут, tz Asia/Krasnoyarsk — QA через agent-browser и автономное развитие проекта
- Удалены временные файлы исследования (app.js, vendors.js, html-дампы); в research/ оставлены только PDF-образцы меню и JSON результатов поиска
- Финальная верификация: главная HTTP 200, /api/menu HTTP 200, в dev.log 0 ошибок (grep ⨯|Error = 0), lint чистый

Stage Summary:
- ПРОЕКТ ЗАВЕРШЁН: аналитический документ (docs/sunc-info-analysis.md) + FastAPI-эталон (fastapi-backend/, протестирован) + рабочий Next.js-портал (port 3000, все парсеры на реальных данных)
- Следующие итерации (для webDevReview): Telegram-бот на aiogram поверх парсеров fastapi-backend; RSS-лента новостей; кэширование меню в БД; поиск по блюдам; PWA-манифест; уведомления об изменении меню

---
Task ID: 6
Agent: main (Z.ai Code)
Task: QA-аудит через agent-browser + VLM, исправление багов, новые фичи (поиск/фильтры меню, PWA, аналитика питания), улучшение стилей

Work Log:
- QA-аудит: все 9 вкладок, 12 API-эндпоинтов (200), мобайл 390px без overflow, тёмная тема, dev.log без ошибок, lint чистый — стабильность подтверждена
- VLM-ревью скриншотов (dashboard/canteen/mobile/dark): оценка B+, получен список улучшений (БЖУ-пилюли, подсветка текущей пары, hover-эффекты, контраст, touch-таргеты)
- НОВАЯ ФИЧА «Поиск и фильтры меню» (canteen.tsx + nutrition.ts): поиск по названию+составу с подсветкой <mark>, 7 фильтров-чипов (вегетарианское, высокобелковые, без молока/глютена/яиц/рыбы/орехов), авто-детекция аллергенов (молоко/глютен/яйца/рыба/орехи — иконки с tooltip на каждом блюде), счётчик «Найдено N из M», навигация по датам ‹ › + кнопка «Сегодня», «Поделиться» (navigator.share → clipboard с тостом)
- НОВАЯ ФИЧА «Аналитика» (10-я вкладка, analytics.tsx): CSS-график калорий за 10 дней (сегодня/max/min подсвечены, пунктир средней), карточки Среднее/Минимум/Максимум с БЖУ-легендой, распределение ккал по приёмам пищи (прогресс-бары), калькулятор «Мой выбор» — чекбоксы блюд, живые итоги, localStorage (hydration-safe через useSyncExternalStore)
- НОВЫЙ API /api/menu/stats?days=N (menuStats.ts): агрегация за N дней, Promise.allSettled, общий кэш с /api/menu (ключи menu:дата), кэш каталога (menuCatalog, TTL 1ч — было 15 загрузок каталога на 15 дат, стала 1)
- НОВАЯ ФИЧА PWA: сгенерирована иконка (z-ai image → sharp → 192/512/maskable/apple-touch), manifest.webmanifest (shortcuts ?tab=, theme #f59e0b), service worker v2 (network-first для навигации и чанков, cache-first для иконок, офлайн-фолбэк), офлайн-страница offline.html (тёплый дизайн, авто-reload при online), регистрация SW + metadata (manifest, appleWebApp, themeColor, OpenGraph)
- Горячие клавиши Alt+1…0 для 10 вкладок (не срабатывают в input/textarea), deep-links ?tab=… (PWA-шорткаты), прогрев кэша статистики с дашборда
- СТИЛИ: MacroPills (Б=emerald, Ж=amber, У=teal) в dashboard+canteen+analytics; подсветка текущей пары в списке звонков; hover-лифт карточек (translateY -0.5 + shadow) во всех секциях; контраст dark muted-foreground 0.712→0.735; uppercase-заголовки приёмов пищи с разделителем; анимация стрелки в новостях; focus-visible ring в навигации; ::selection, prefers-reduced-motion, scroll-behavior
- ИСПРАВЛЕНЫ БАГИ: (1) все столбцы графика одинаковой высоты — flex-shrink сжимал %-высоты, переделано на absolute-in-track; (2) SW v1 кэшировал dev-чанки cache-first → браузер получал СТАРЫЙ JS, переделано на network-first + VERSION bump + очистка кэша; (3) lint react-hooks/set-state-in-effect ×2 — deep-link через setTimeout-колбэк, localStorage через useSyncExternalStore
- Финальная верификация: 12 эндпоинтов 200 (7–43ms после прогрева), lint чистый, dev.log 0 ошибок, мобайл 390px без overflow (canteen с фильтрами + analytics с графиком), VLM-оценка дашборда A-

Stage Summary:
- Портал: 10 вкладок, 13 API-роутов (добавлен /api/menu/stats), PWA устанавливаемое приложение, поиск/аллергены/калькулятор калорий
- Первый запрос /api/menu/stats ~30–40с (10 PDF), далее мгновенно из кэша; прогрев запускается с дашборда автоматически
- Известные ограничения: clipboard в headless-браузере запрещён (тост «Не удалось поделиться» — корректно для sandbox, в реальном браузере работает); дежурства/вожатые — по-прежнему демо-данные БД (источник только офлайн)
- Следующие итерации: Telegram-бот (aiogram поверх парсеров), RSS новостей, кэширование меню в БД (персистентность между рестартами), поиск по учителям в расписании, push-уведомления PWA

---
Task ID: 7
Agent: main (Z.ai Code)
Task: Мобильный вид (фикс), перенос «Аналитики» в «Столовую», PWA-установка как приложение, Telegram-бот, группы в расписании

Work Log:
- QA мобайл 390px: найден overflow 463px на вкладке «Расписание» — ПРИЧИНА: у сетки дней не было базового `grid-cols-1`, неявный grid-трек `auto` раздувался под min-content неразрывного текста (строка «Основы безопасности…» + списки классов). Исправлено: `grid-cols-1` добавлен ВСЕМ адаптивным сеткам во всех 10 секциях (python-скрипт, 13 замен) + `min-w-0`/`truncate`/`break-words` в карточках уроков
- МОБИЛЬНЫЙ ВИД: подписи вкладок теперь видны всегда (не только sm+), компактная шапка (дата «Пн, 31.08.2026», логотип 40px, px-3), main/footer отступы px-3 py-4 на мобиле; VLM-проверка дашборда/столовой/расписания — без критических проблем, тач-таргеты ≥44px
- ГРУППЫ В РАСПИСАНИИ (новый запрос пользователя): уроки с одинаковым begin–end = параллельные группы. Новый алгоритм groupBySlots(): слоты с 1 уроком — компактная строка; слоты с N уроками — блок «класс делится на N группы» (иконка GitBranch) с сеткой пронумерованных карточек (номер в кружке, предмет, учитель, кабинет); счётчик дня = число слотов (было lessons.length — завышено), бейдж «N×группы» в заголовке дня, легенда-подсказка под селектором класса
- ПЕРЕНОС «АНАЛИТИКИ»: вкладка удалена из TABS (9 вкладок), внутри «Столовой» — сегментированный переключатель «Меню | Аналитика» (role=tablist, aria-selected). Состояние canteenView в app.tsx; deep-link ?tab=analytics → canteen+analytics (проверено); кнопка дашборда «Аналитика →»; Alt+1…9; тексты в «Инфо» обновлены
- PWA-УСТАНОВКА («собери приложение для телефона»): src/lib/pwa-install.ts (beforeinstallprompt-стор на useSyncExternalStore, initPwaInstall в PwaRegister); InstallAppButton в шапке (появляется при доступной установке); InstallBannerCard на дашборде (диалог установки / инструкция iOS «Поделиться → На экран Домой», localStorage-дизмисс, скрыт в standalone); manifest: +id, +2 скриншота narrow (390x844 из реальных QA-скриншотов через sharp → public/screenshots/) для rich install dialog; SW VERSION v2→v3
- TELEGRAM-БОТ: mini-services/tg-bot/ (bun-проект, grammY, порт 3003, bun --hot). Команды: /start /help /menu [дата] /bells /schedule [класс] /tomorrow /weather /news /duty /counselors /info; HTML-форматирование с экранированием, typing-индикатор, setMyCommands; расписание с той же логикой групп («класс делится на N группы»); без TELEGRAM_BOT_TOKEN — спящий режим с health-сервером /health; функции форматирования экспортируются и протестированы на реальных данных (меню 2866 ккал, звонки 13, погода, дежурства, вожатые, инфо); README.md с инструкцией BotFather
- ИСПРАВЛЕНЫ БАГИ: (1) /api/news?limit=N — ключ кэша включал limit → каждый новый limit дёргал sesc.nsu.ru и падал при флуктуации сети; теперь кэшируется news:all (25 шт) и срезается по limit; (2) fetchWithTimeout: +2 ретрая с backoff 500/1000мс, повтор и на 5xx; (3) bot: Map.size вместо Map.length («Уроков по сетке: undefined»); (4) контраст подписей графика калорий в тёмной теме (foreground/60-75 вместо muted-foreground)
- ВЕРИФИКАЦИЯ: все 9 вкладок 390px без overflow; deep-link ?tab=analytics работает; Alt+4 → Дежурства; тёмная тема ок; консоль и page errors пусты (ошибки Fast Refresh mid-edit исчезли после reload); lint чистый; dev.log 0 ошибок; портал :3000 200, бот :3003 health ok

Stage Summary:
- Мобильный вид починен (overflow 463→390), «Аналитика» перенесена в «Столовую» переключателем сверху, PWA-установка оформлена (кнопка в шапке + баннер + скриншоты в манифесте), Telegram-бот полностью реализован и работает в спящем режиме до ввода токена
- Расписание теперь корректно отражает деление класса на группы (параллельные уроки) — и на сайте, и в боте
- Сервисы: Next.js :3000 (dev), tg-bot :3003 (bun --hot, ждёт TELEGRAM_BOT_TOKEN)
- Известные ограничения: (1) у бота нет реального токена — нужен BotFather; (2) дежурства/вожатые — демо-данные БД; (3) beforeinstallprompt не срабатывает в headless (в реальном Chrome работает); (4) первый /api/menu/stats ~30с (прогрев)
- Следующие итерации: инлайн-клавиатура дат меню в боте + подписка на «меню на завтра»; пуш-уведомления PWA; кэширование меню в БД; поиск по учителям в расписании; уроки «сейчас» в шапке расписания

---
Task ID: 8
Agent: main (Z.ai Code)
Task: Гайд по использованию портала (запрос: «Напиши гайд как этим пользоваться»)

Work Log:
- НОВАЯ 10-Я ВКЛАДКА «Гайд» (sections/guide.tsx): 7 глав — (1) Начало работы: источники данных, бейдж «из кэша», навигация; (2) Разделы портала: 9 карточек с буллетами и кнопкой «Открыть» (навигация через onNavigate); (3) Установка на телефон: шаги Android/Chrome (⋮ → Установить) и iOS/Safari (Поделиться → На экран „Домой“), что даёт PWA (4 тайла); (4) Telegram-бот: таблица 10 команд + заметка про группы + инструкция BotFather (TELEGRAM_BOT_TOKEN, mini-services/tg-bot); (5) Горячие клавиши: таблица Alt+1…0, чипы-диплинки ?tab=… (кликабельны); (6) Администраторам: X-Admin-Key (по умолчанию sunc-admin), curl-пример POST /api/duty; (7) FAQ: 7 вопросов-аккордеонов (кэш, медленный первый прогрев, localStorage «Мой выбор», меню на завтра, группы, расхождения, обратная связь). Финальная карточка с кнопками «На главную / Что на обед? / Расписание»
- ИНТЕРАКТИВ: оглавление-чипы со smooth-scroll к якорям (scroll-mt-36 под шапку), кнопки «Открыть» в карточках разделов, чипы ?tab= — навигация, FAQ Accordion (single collapsible)
- ПО РЕКОМЕНДАЦИИ VLM: оглавление вынесено из героя в ЛИПКУЮ ПАНЕЛЬ глав (sticky top-[110px]/sm:top-[120px] — замерено по фактической высоте шапки) с horizontal-scroll чипами (shrink-0, no-scrollbar), SCROLL-SPY подсветкой активной главы (IntersectionObserver, rootMargin -130px/-55%) и ПРОГРЕСС-БАРОМ чтения (пассивный scroll-listener, transition width); фон background/90 + backdrop-blur, на мобиле full-bleed (-mx-3 border-y), на десктопе плавающая пилюля (rounded-2xl border shadow)
- app.tsx: вкладка «Гайд» (BookOpen, 10-я), горячие клавиши Alt+1…9 → Alt+1…0 (Alt+0 = Гайд), рендер GuideSection с onNavigate, футер «горячие клавиши: Alt+1…0 · гайд — вкладка «Гайд»»
- info.tsx: текст клавиш обновлён + отсылка к вкладке «Гайд»; manifest.webmanifest: PWA-шорткат «Гайд» (/?tab=guide)
- ВЕРИФИКАЦИЯ (agent-browser): 10 вкладок в навигации; гайд рендерится; клик по чипу TOC скроллит к главе (guide-bot top=164px); FAQ раскрывается (data-state=open); «Открыть» → переход в «Расписание»; sticky-панель: top 110px мобайл / 120px десктоп (замерено), scroll-spy «На телефон» на середине страницы, прогресс 0%→47%; deep-link ?tab=guide работает; мобайл 390px scrollW=390 (без overflow, верх/середина/низ); футер прижат естественно (bottom=0px); консоль без ошибок; dev.log без ошибок; lint чистый
- VLM-ревью: desktop — «отличная читабельность, консистентность высокая, критических проблем нет»; повторная проверка по рекомендации — внедрена липкая панель; ложные срабатывания VLM (логотип «за экраном») опровергнуты точными замерами (logo 12–52px при viewport 390)

Stage Summary:
- Гайд встроен в портал: вкладка «Гайд» (Alt+0, deep-link ?tab=guide, PWA-шорткат), 7 глав с интерактивной навигацией, липкая панель глав с scroll-spy и прогресс-баром чтения
- Портал теперь самодокументирован: новичок осваивает всё за 5 минут без внешних инструкций
- Риски/ограничения: липкая панель рассчитана на текущую высоту шапки (110/120px — при изменении шапки поправить top-[]); VLM-скриншот-ревью склонно к ложным срабатываниям на скроллируемых контейнерах — верифицировать замерами
- Следующие итерации: печатная версия гайда (PDF), встроенный тур-обзор для первого визита (onboarding-подсказки), кнопка «Гайд» в пустых состояниях

---
Task ID: 9
Agent: main (Z.ai Code)
Task: Интеграция Google-таблицы «Мероприятия 2026-2027» (запрос пользователя) + обновление гайда

Work Log:
- АНАЛИЗ ИСТОЧНИКА: docs.google.com/spreadsheets/d/1t8CeC4UvOJrNgXI48fKju1q4iEXJ7bAF (gid=718067972, публичный CSV-экспорт). Структура: строка-заголовок («дата | день недели | ЕГЭ/ОГЭ и др. | 8-1 … 11-12 | дата | день недели»), блок «Мероприятия 2026-2027 учебного года» = календарь 28.08.2026→31.12.2027 (596 событий: 110 общих + по классам), ниже — пустые архивные шаблоны («2024-2025», правая половина — календарь без событий, игнорируется)
- БЭКЕНД: src/lib/server/events.ts — парсер (RFC-4180 CSV с кавычками; год через переход дек→янв инкрементируется; парсинг заголовка блоков «Мероприятия YYYY-YYYY»; стоп на втором заголовке = архивные блоки; события ячейки делятся по «;»; день недели вычисляется из даты). API-роут /api/events (TTL 30 мин, stale-режим через cached()). Живой тест: 187 дней, 597 событий, 25 классов, последнее — «резерв ЕГЭ» 06.07.2027 ✓
- ФРОНТЕНД: НОВАЯ 11-Я ВКЛАДКА «Мероприятия» (sections/events.tsx, иконка CalendarClock, после «Расписания»): карточка-заголовок со счётчиками (всего/впереди) и ссылкой на таблицу; фильтры — Select класса (26 опций), поиск (по тексту и дате), Switch «Прошедшие»; прикреплённая карточка «Ближайшее мероприятие» (крупная дата, «через N дн.», топ-3 события); лента по месяцам (11 групп сент.2026→июль 2027) с карточками событий (значок 📣 общее / 👥 класс, чип класса, «сегодня»-бейдж); кнопка «Наверх»; EmptyState для пустых фильтров
- ДАШБОРД: карточка «Ближайшие мероприятия» (3 события, иконки, «Календарь →» в events)
- TELEGRAM-БОТ: команда /events [класс] (ближайшие 8 с группировкой по датам, 📣/👥, ссылка на таблицу; форматер eventsText протестирован на живых данных: «1 сентября — Линейка, посвященная Дню знаний…»), setMyCommands, WELCOME, README
- ГАЙД ОБНОВЛЁН: глава 1 «Данные живые» + Google-таблица; глава 2 — карточка «Мероприятия» (10 вкладок, «Десять вкладок» в подзаголовке — было «Девять»); глава 4 — /events в таблице команд + пример в инструкции подключения; глава 5 — карта клавиш переделана (Alt+1…9 → вкладки 1–9, Alt+0 → Гайд, Документ — мышкой/диплинком), чип-диплинк events
- ПРОЧЕЕ: hotkeys app.tsx Alt+0 → TABS.length-1 (Гайд); SCHOOL_INFO.links + «Календарь мероприятий школы» (Google-таблица); manifest-шорткат «Мероприятия» (/?tab=events); info.tsx + строка «Календарь мероприятий»; футер и подпись дашборда + Google Sheets
- ИСПРАВЛЕНЫ В ПРОЦЕССЕ: дубль SECTION_DOCS «events» после частично применённого MultiEdit (дубль вызывал React key-ошибку в консоли — убран, в свежей загрузке 0 ошибок)
- ВЕРИФИКАЦИЯ: 11 вкладок; ?tab=events deep-link ✓; фильтр класса 10-4 (37 совпадений, чужих классов нет) ✓; поиск «ЕГЭ» (47 совпадений) ✓; Switch «Прошедшие» (598 строк, появился «Август 2026», интенсив/пересдача) ✓; мобайл 390px — ВСЕ 11 вкладок 390/390 без overflow; консоль последней загрузки чистая; lint чистый; dev.log без ошибок; бот :3003 health ok, модуль импортируется; VLM-ревью вкладки — положительное (иерархия чёткая, палитра консистентна, дефектов вёрстки нет)

Stage Summary:
- Новый источник данных интегрирован сквозно: Google-таблица → парсер (TTL 30 мин) → /api/events → вкладка «Мероприятия» с фильтрами → дашборд-карточка → /events в TG-боте → гайд/инфо/манифест
- Мероприятия 2026-2027: 597 событий, 187 дней, общие (ЕГЭ/ОГЭ) + персональные по 25 классам
- Ограничения: (1) таблица должна оставаться публичной (CSV-экспорт без авторизации); (2) если школа сменит структуру листа (столбцы/заголовки), парсер вернёт понятную ошибку → retry/stale; (3) события июля 2027+ в блоке B могут принадлежать следующему году — отображаются как есть
- Следующие итерации: подписка на «мероприятие завтра» в боте; фильтр «только общие»; экспорт в календарь (ICS); сравнение списка классов таблицы с table-sesc

---
Task ID: 9
Agent: Z.ai Code (main)
Task: Написать в чат гайд по хостингу и развёртыванию «СУНЦ Инфо» (нужен ли свой сервер, как поднять, можно ли бесплатно)

Work Log:
- Изучены артефакты деплоя: package.json (build → standalone, start → bun .next/standalone/server.js), next.config.ts (output: "standalone"), prisma/schema.prisma (sqlite), mini-services/tg-bot (grammY long polling, health 3003, PORTAL_API), Caddyfile (песочница-only gateway), env: DATABASE_URL / ADMIN_KEY / TELEGRAM_BOT_TOKEN
- Проверено: z-ai-web-dev-sdk в src/ не используется → проект полностью самодостаточен для внешнего хостинга
- Составлен гайд в чате: 4 компонента для хостинга; 3 пути (A: Vercel+Neon 0₽; B: Oracle Always Free / домашний сервер + Cloudflare Tunnel 0₽; C: VPS со systemd + Caddy + crontab); команды, systemd-юниты, Caddyfile, крон прогрева/бэкапа, таблица стоимости, чек-лист
- Cron: удалён отключённый 348103, создан 348161 (webDevReview, каждые 15 мин, Asia/Krasnoyarsk)

Stage Summary:
- Код не менялся; задача документационная (гайд в чат)
- Ключевой вывод: SQLite-файл + long polling бота → проще всего VPS/домашний сервер (SQLite и polling работают «как есть»); на serverless нужны доработки: provider postgresql (Neon) или Turso, бот → webhook-роут, keepalive через GitHub Actions
- Риски/next: если пользователь выберет Vercel-путь — нужна доработка (миграция Prisma-провайдера, webhook-роут бота, in-memory кэш короткоживущий на lambda)

---
Task ID: 10
Agent: Antigravity (Google Deepmind)
Task: Глубокий аудит проекта, интеграция rasp.jpg (график смен столовой), исправление парсинга лекций/семинаров/спецкурсов, исправление целевых классов мероприятий, модернизация Telegram-бота

Work Log:
- АУДИТ И БАЗА ДАННЫХ:
  • Обнаружен и исправлен критический баг путей в `.env`: `DATABASE_URL` указывал на несуществующий `/home/z/my-project/db/custom.db` → исправлен на актуальный путь к `custom.db`.
  • Решена проблема работы Prisma Client на NixOS: загружены и подключены нативные бинарники `schema-engine` и `libquery_engine.so.node` в `.prisma-engines/`, выполнены `prisma db push` и генерация клиента.
  • Составлен подробный аналитический отчёт в файле `docs/project-audit.md`.
- ИНТЕГРАЦИЯ rasp.jpg (ГРАФИК СМЕН СТОЛОВОЙ СО 2 СЕНТЯБРЯ):
  • Оцифрованы все данные: 1-я смена (8-1, 11-1..9), 2-я смена (10-е классы), 3-я смена (9-е классы и 11-10), дежурные по столовой, опоздавшие, будни и выходные дни.
  • Создан серверный модуль `src/lib/server/canteenSchedule.ts` с расчётом персональных смен и живого статуса `getCurrentMealState()`.
  • Создан API-маршрут `/api/canteen/schedule`.
  • В интерфейс портала (раздел «Столовая») добавлен интерактивный переключатель `Меню | График смен | Аналитика` с компонентом `CanteenScheduleView` (сводная таблица, выбор своего класса, карточка живого статуса).
  • На дашборд добавлена строчка «Сейчас в столовой» и переход к графику смен.
- ИСПРАВЛЕНИЕ КЛАССИФИКАЦИИ ЗАНЯТИЙ (ЛЕКЦИИ / СЕМИНАРЫ / СПЕЦКУРСЫ):
  • Исследована кодовая база `table-sesc.nsu.ru` (бандл `app.b5484b70.js`): выяснено, что `type: 1` = Лекция («лек»), `type: 2` = Семинар/Практика («пр»), `type: 3` = Лабораторная («лаб»).
  • Устранено ошибочное предположение в `tableSesc.ts` и `fastapi-backend/parsers/schedule_parser.py` (ранее все семинары ложно помечались как «Спецкурс»).
  • Добавлен интеллектуальный классификатор `classifyLesson`, распознающий спецкурсы и факультативы по названию.
- ИСПРАВЛЕНИЕ ЦЕЛЕВЫХ КЛАССОВ В МЕРОПРИЯТИЯХ (Google Sheets):
  • Алгоритм `resolveTargetClasses` в `src/lib/server/events.ts`:
    1) Явный поиск номеров классов в скобках/тексте: `(11-1,11-2, 11-3)`, `11-5,6`, `9-1,2,3`, `11-8` (устранена потеря событий в классах 11-2, 11-3 и т.д.).
    2) Распознавание параллелей: `ОГЭ` → 9 классы, `ЕГЭ / КЕГЭ` → 11 классы, 10 классы.
    3) Распознавание общешкольных событий (КАНИКУЛЫ, ДЕНЬ САМОУПРАВЛЕНИЯ, Посвящение в ФМШата, Генеральная уборка в общежитиях, Презентация спецкурсов) — теперь они видны всем классам, а не только 8-1/9-1.
    4) Категоризация каждого события: `Лекция`, `Спецкурс`, `Семинар / Консультация`, `Экзамен / Пересдача`, `Уборка`, `Мероприятие`.
- МОДЕРНИЗАЦИЯ TELEGRAM-БОТА (`mini-services/tg-bot/`):
  • Добавлена команда `/canteen` (и `/canteen <класс>`) с графиком смен питания и инлайн-кнопками.
  • Добавлена команда `/now` («Что сейчас в школе»: звонок, столовая, погода, дежурный класс).
  • Добавлены команды `/setclass <класс>` и `/myclass` для запоминания класса пользователя в сессии.
  • Добавлена команда `/find <учитель|кабинет>` для поиска расписания преподавателей и аудиторий.
  • В `/schedule` добавлены инлайн-кнопки переключения дней недели (`Пн`..`Сб`) и корректные теги `[Лекция]`, `[Семинар]`, `[Спецкурс]`.
  • В `/menu` добавлены инлайн-кнопки переключения дат.
  • Добавлена постоянная удобная Reply-клавиатура (кнопки внизу экрана для работы в 1 клик).
  • Обновлён `README.md` бота.

Stage Summary:
- Все 5 требований пользователя выполнены полностью и проверены на живых данных.
- Линтер `eslint .` чистый (0 ошибок), сборка и тесты модулей завершились успешно.

---
Task ID: 11
Agent: Antigravity (Google Deepmind)
Task: Редизайн форматирования сообщений Telegram-бота (использование всех возможностей форматирования Telegram: лаконичные списки, bold-заголовки, blockquote, expandable blockquote, code)

Work Log:
- /menu: Полный редизайн вывода меню столовой согласно запросу пользователя (лаконичный заголовок «Меню на DD.MM», чёткие блоки «Завтрак:», «Обед:», аккуратные маркеры блюд «• », скрытие громоздких граммовок/калорий из основного текста блюд; сводка КБЖУ и ссылка на PDF вынесены в современный скрывающийся блок `<blockquote expandable>`).
- /bells: Лаконичное расписание 3 основных пар с точными интервалами `<code>` и переменами; вторая половина дня (спецкурсы, обед, самоподготовка) вынесена в `<blockquote expandable>`.
- /canteen: Статус столовой прямо сейчас оформлен через `<blockquote>`, расписание смены класса и общие приёмы пищи приведены к чистому списку с отметками дежурств/опоздавших.
- /schedule: Структурированный вывод пар и полупарков без лишнего визуального шума и псевдографики; параллельные группы и окна оформлены чисто; вечерние спецкурсы скрыты под `<blockquote expandable>`.
- /now: Сводный дашборд «Сейчас в школе» (урок, столовая, погода, дежурный класс) упакован в единый элегантный `<blockquote>`.
- /weather, /events, /duty, /counselors, /info, /stats: Очищены от громоздких разделителей `─────`, структурированы с помощью `<blockquote>` и `<blockquote expandable>`.
- Добавлена поддержка команды `/news` для просмотра последних новостей СУНЦ НГУ с кликабельными ссылками.
- Все команды зарегистрированы в меню бота через `setMyCommands`.
- Протестирован рендеринг на живых данных API.

Stage Summary:
- Форматирование всех сообщений Telegram-бота полностью обновлено и оптимизировано для быстрого чтения со смартфона.

---
Task ID: 12
Agent: Antigravity (Google Deepmind)
Task: Форматирование номеров аудиторий с точкой (3_4 -> 3.4) и система раздельного выбора подгрупп (по предметам и по английскому языку)

Work Log:
- АУДИТОРИИ:
  • Обновлен парсер/клиент table-sesc (`src/lib/server/tableSesc.ts`): upstream-запросы транслируют точку в подчеркивание (`filter.classroom.replace(/\./g, "_")`), а возвращаемые номера нормализуются в точечный формат (`3.4`, `4.2`).
  • Обновлен веб-интерфейс (`src/components/sunc/sections/schedule.tsx`): отображение аудиторий переведено на формат с точкой (`{lesson.classroom.replace(/_/g, ".")}`).
  • В Telegram-боте (`mini-services/tg-bot/index.ts`): добавлена функция `fmtClassroom(room)` с заменой `_` на `.`. Поддерживается поиск и отображение по запросам `3.4`, `/find 3.4`, `3.8` и т.д.
- ПОДГРУППЫ И АНГЛИЙСКИЙ ЯЗЫК:
  • Добавлено разделение на общие предметы (1-я / 2-я подгруппа) и группы по иностранному языку (Группа I-IV с фамилией преподавателя).
  • Создан механизм `getEnglishGroupsForClass(className)` для извлечения групп и преподавателей иностранного языка конкретного класса.
  • Реализована фильтрация `lessonMatchesUser(lesson, userSub, userEng)`: индивидуальное расписание показывает только занятия выбранной подгруппы и группы английского языка, скрывая чужие и лаконично маркируя «Окно у твоей подгруппы».
  • Добавлены интерактивные меню выбора: `/subgroup` (`/subgroups`, `/group`) с кнопками переключения 1-я / 2-я подгруппа / Все, и отдельным меню по английскому языку (`subgroup:eng:idx:...`).
  • Сохранение выбора пользователя: персистентность в `user_subgroups.json` и `users.json`.
  • В расписание добавлена кнопка быстрого переключения между «👤 Моя подгруппа» и «👥 Весь класс».

Stage Summary:
- Номера аудиторий везде выводятся в формате 3.4 вместо 3_4.
- Выбор подгрупп и английского языка полностью функционирует в Telegram-боте с интерактивным интерфейсом и персистентным хранением.

---
Task ID: 13
Agent: Antigravity (Google Deepmind)
Task: Обновление индексации пользователей для сохранения подгруппы в базе данных (Prisma SQLite)

Work Log:
- СХЕМА БАЗЫ ДАННЫХ (`prisma/schema.prisma`):
  • В модель `TelegramUser` добавлены поля `subgroup Int?` (1 или 2), `englishGroup String?` и поисковый индекс `@@index([subgroup])`.
  • В модель `WebVisitor` добавлено поле `subgroup Int?` и поисковый индекс `@@index([subgroup])`.
  • Выполнены миграция `prisma db push` и генерация клиента `prisma generate`.
- БЭКЕНД И API СИНХРОНИЗАЦИИ:
  • `POST /api/users/telegram`: расширен payload (поля `subgroup`, `englishGroup`), сохранение и обновление в БД, детальный аудит.
  • `GET /api/users/telegram`: добавлена фильтрация по `subgroup` (`?subgroup=1|2`), выдача подгрупп в списке пользователей и подсчёт статистики `bySubgroup` ("1", "2", "none").
  • `GET /api/users/stats`: в выборку `recentUsers` добавлены поля `subgroup` и `englishGroup`, в агрегацию включен блок `bySubgroup`.
  • `POST /api/users/web`: сохранение `subgroup` посетителей сайта.
- TELEGRAM-БОТ (`mini-services/tg-bot/index.ts`):
  • `trackUserInteraction`: отправка `subgroup` и `englishGroup` при любых взаимодействиях.
  • Создана функция `syncUserToDb(userId, actionName)` для мгновенной синхронизации при переключении подгрупп.
  • Интегрирован вызов `syncUserToDb` в колбэки выбора основной подгруппы и группы по английскому языку.
  • Реализована функция `backfillUsersToDb()`: фоновая синхронизация всех пользователей из локального кэша в БД при старте бота.
- ПАНЕЛЬ АДМИНИСТРАТОРА И АНАЛИТИКА (`src/components/sunc/sections/users-stats.tsx`):
  • В карточку «Выбрали класс» добавлено отображение распределения по подгруппам.
  • Добавлен переключатель фильтра подгрупп (Все подгр., 1-я, 2-я).
  • В реестр пользователей добавлена колонка «Подгруппа» с цветными бейджами (`1-я подгр.` / `2-я подгр.`) и отметкой группы по английскому языку.
  • Полнотекстовый поиск расширен для поддержки поиска по названию группы английского языка и подгруппе.

Stage Summary:
- Индексация пользователей полностью обновлена: выбор подгруппы и группы английского языка надежно сохраняется в базе данных SQLite и доступен администраторам.
